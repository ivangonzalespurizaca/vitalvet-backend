spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: MICRO-AGENDA
          uri: lb://MICRO-AGENDA
          predicates:
            - Path=/api/agenda/**
        - id: MICRO-PACIENTE
          uri: lb://MICRO-PACIENTE
          predicates:
            - Path=/api/paciente/**
        - id: MICRO-USUARIO
          uri: lb://MICRO-USUARIO
          predicates:
            - Path=/api/usuario/**
        - id: MICRO-SOPORTE
          uri: lb://MICRO-SOPORTE
          predicates:
            - Path=/api/soporte/**
server:
  port: 8094
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost