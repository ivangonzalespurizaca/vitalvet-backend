server:
  port: 8095
spring:
  kafka:
    bootstrap-servers: 127.0.0.1:9092
    consumer:
      group-id: auditoria-grupo-limpio-final
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.apache.kafka.common.serialization.StringDeserializer
  data:
    mongodb:
      auto-index-creation: true
  mongodb:
    uri: mongodb+srv://jhonramos3456_db_user:admin@cluster0.pdcbohm.mongodb.net/vitalvet_auditoria?retryWrites=true&w=majority
  messages:
    basename: messages
  application:
    name: MICRO-AUD
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    org.springframework.kafka: DEBUG